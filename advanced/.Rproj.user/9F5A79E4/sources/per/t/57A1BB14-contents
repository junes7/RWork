install.packages("beepr")
#명령어를 올리는 작업
library("beepr")
beep()
for(i in 1:10){
  beep(sound = i)
  Sys.sleep(1) #1초동안 프로세스를 동작시키지 않겠다는 의미이다.
}
#저장된 라이브러리 경로를 보여준다.
.libPaths()

#설치된 package
#sqldf, readxl, beepr, data.table, ggplot2, splitstackshape, XML
install.packages("dplyr")
